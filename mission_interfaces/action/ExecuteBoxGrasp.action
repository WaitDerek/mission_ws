# Execute a dual-arm box grasp in the robot-configured execution frame.
string request_id
int32 target_label
bool dry_run

---

bool success
string message
# Geometric-center box pose transformed into the configured execution frame.
geometry_msgs/PoseStamped box_pose
float64 score
float64 width
float64 height
int32 object_id
string pickup_message
bool gripper_command_published
bool torso_lift_command_published

---

string stage
string detail
